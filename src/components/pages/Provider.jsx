import { ProviderHero } from "../providers/ProviderHero";

const Provider = ({ provider }) => {

    return (
        <>
            <ProviderHero provider={provider} />
        </>
    )
}

export default Provider;
